[project]
name = "construct"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40.0",
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests/e2e"]
addopts = "-v"

[dependency-groups]
dev = [
    "bcrypt>=5.0.0",
]
